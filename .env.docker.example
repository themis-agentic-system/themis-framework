# Themis Framework - Docker Environment Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# API Authentication
# =============================================================================
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# For local development, leave commented out for development mode (no auth required)
# For production, uncomment and set a secure API key
# THEMIS_API_KEY=your-secret-api-key-here

# =============================================================================
# LLM Provider (Anthropic Claude)
# =============================================================================
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Claude model version (optional, defaults to claude-sonnet-4-5)
# MODEL=claude-sonnet-4-5

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL password (change in production!)
POSTGRES_PASSWORD=your-secure-postgres-password

# Full database URL (automatically constructed in docker-compose)
# DATABASE_URL=postgresql://themis:password@postgres:5432/themis

# =============================================================================
# Application Settings
# =============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# State cache TTL in seconds (default: 60)
CACHE_TTL_SECONDS=60

# =============================================================================
# Monitoring (Optional)
# =============================================================================
# Grafana admin password
GRAFANA_PASSWORD=admin

# =============================================================================
# Production Settings
# =============================================================================
# Set to 'true' in production for additional security
# PRODUCTION_MODE=false

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Maximum request size in MB
# MAX_REQUEST_SIZE_MB=10

# =============================================================================
# Notes
# =============================================================================
# 1. Never commit this file with real secrets to git
# 2. Use strong, randomly generated passwords in production
# 3. Rotate API keys regularly (every 90 days recommended)
# 4. Enable HTTPS/TLS in production
# 5. Configure firewall rules to restrict database access
